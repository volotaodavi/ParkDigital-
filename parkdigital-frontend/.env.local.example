# URL do backend ParkDigital (Express). O `next dev` já ocupa a porta 3000,
# então rode o backend em outra porta (ex: PORT=4000) durante o
# desenvolvimento local.
NEXT_PUBLIC_API_URL=http://localhost:4000
